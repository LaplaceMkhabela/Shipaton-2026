import { Placeholder } from '@/components/ui/placeholder';

// Lane B. Pick a video, attach it to a course, publish.
export default function CreateScreen() {
  return <Placeholder title="Create" note="Upload a lesson" />;
}
